RewriteEngine on
RewriteBase /

# Redirect legacy query-string URLs back to clean paths
RewriteCond %{QUERY_STRING} ^path=([^&]+)$ [NC]
RewriteRule ^$ /%1? [R=302,L]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [NC,L,QSA]
